# Visual Processing Component Requirements
# Includes base requirements plus visual-specific dependencies

-r base.txt

# Object Detection and Tracking
ultralytics>=8.0.0
supervision>=0.16.0

# Face Detection and Recognition
insightface>=0.7.3
onnxruntime-gpu>=1.16.0  # Use onnxruntime for CPU-only
face-recognition>=1.3.0
dlib>=19.24.0
mtcnn>=0.1.1
# retinaface removed - has hardcoded old TensorFlow dependencies incompatible with Python 3.12
# Use YOLO or mediapipe for face detection instead

# Emotion Detection
deepface>=0.0.79
fer>=22.5.1

# Face Analysis
mediapipe>=0.10.0
facenet-pytorch>=2.5.3

# Image Processing
albumentations>=1.3.1
# imgaug has Python 3.12 compatibility issues, using albumentations instead

# Tracking
# sort-track doesn't have version 1.0.0, using latest available
sort-track>=0.0.8
deep-sort-realtime>=1.5.0