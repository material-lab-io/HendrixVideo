# Hendrix Minimal Requirements
# Bare minimum for running tests and basic functionality

# Core dependencies only
numpy>=1.26.0
opencv-python>=4.8.0
pillow>=10.0.0
pyyaml>=6.0
pytest>=7.4.0
pytest-asyncio>=0.21.0
tqdm>=4.65.0